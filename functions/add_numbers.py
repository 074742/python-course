# this is the function that add_numbers 
    
def add_numbers(fir_num,sec_num):
    add_numbers=fir_num+sec_num
    return add_numbers
#add_numbers(fir_num,sec_num):
Total = add_numbers(10,5)
print(Total)